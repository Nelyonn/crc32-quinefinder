name: compile

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: Upload binaries
      uses: actions/upload-artifact@v7.0.1
      with:
        name: binaries
        path: "findquines-crc32*"
